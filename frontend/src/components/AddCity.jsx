//Component to add a city to the projects

import styled from "styled-components";

//Could I add some sort of form to the side of the main page?
const AddCity = () => {
    return (
        <AddCityStyled>
            AddCity
        </AddCityStyled>
    );
}
const AddCityStyled = styled.button`
    padding-left: 15px;
 `;
export default AddCity;